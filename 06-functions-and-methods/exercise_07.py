# Without running the following code, what do you think it will do?

def foo(bar, qux):
    print(bar)
    print(qux)

foo('Hello')

# It will result in an error
# The function foo have 2 parameters and it expects 2 arguments being passed to it.
# So far there is only 1 argument which is "Hello"